server:
  port: 7001
spring:
  application:
    admin:config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yaojialiu/test-jpa
          search-paths: config-repo